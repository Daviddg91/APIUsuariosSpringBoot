package repositories;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import entidades.User_roles;
/*
@Repository
public interface User_rolesRepository extends JpaRepository<User_roles, Long> {
 
	
	
 
	@Query("select u from User_roles u where user_id = :userId")
	 List findRolesbyUserRolesById(@Param("userId") Long idUsuario);
 
 
}*/
